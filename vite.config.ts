import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// 推し生活OS — React + TypeScript + Vite（Vanilla HTML版 oshi-os-demo からの移行）
export default defineConfig({
  plugins: [react()],
})
